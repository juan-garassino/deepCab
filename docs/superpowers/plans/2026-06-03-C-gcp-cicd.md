# Sub-project C — GCP CI/CD with OIDC + Slack

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship a CI/CD pipeline that mirrors the user's screenshot (build → push → render task → deploy → Slack ✓/✗) but on GCP with Workload Identity Federation (keyless). Default deploy target = Cloud Run; GKE workflow exists but is `workflow_dispatch`-only until the user opts in.

**Architecture:** One reusable workflow (`build-and-push.yml`) does OIDC auth + GAR build/push. Two thin deploy workflows (`deploy-cloud-run.yml`, `deploy-gke.yml`) call the reusable workflow then run the target's deploy command. All Slack notifications via `rtcamp/action-slack-notify@v2.3.3`, single channel `#deepcab-ops` with tag prefixes.

**Tech Stack:** GitHub Actions, `google-github-actions/auth@v2` (Workload Identity Federation), `google-github-actions/setup-gcloud@v2`, gcloud CLI, kustomize, `rtcamp/action-slack-notify@v2.3.3`.

**Reference:** [Design spec §5](../specs/2026-06-03-deepcab-gcp-infra-and-audit-design.md#5-cicd-design-sub-project-c).

**Prerequisite:** Sub-projects A + B landed.

---

## File map

| Action | Path | Purpose |
|---|---|---|
| Create | `.github/workflows/build-and-push.yml` | Reusable: OIDC → GAR build/push |
| Create | `.github/workflows/deploy-cloud-run.yml` | Default deploy, tag-triggered |
| Create | `.github/workflows/deploy-gke.yml` | Dispatch-only |
| Create | `infra/gcp/cloud-run/service.yaml` | Knative service KRM template |
| Create | `infra/gcp/gke/base/deployment.yaml` | k8s Deployment |
| Create | `infra/gcp/gke/base/service.yaml` | k8s Service |
| Create | `infra/gcp/gke/base/configmap.yaml` | k8s ConfigMap |
| Create | `infra/gcp/gke/base/kustomization.yaml` | base kustomization |
| Create | `infra/gcp/gke/overlays/prod/kustomization.yaml` | prod overlay |
| Create | `infra/gcp/workload-identity/README.md` | One-time gcloud commands |
| Create | `infra/gcp/workload-identity/bootstrap.sh` | Optional helper script |
| Modify | `.github/workflows/ci.yml` | update compose paths to `infra/compose/...` |
| Modify | `CLAUDE.md`, `README.md` | document CI/CD + OIDC |

---

## Task C1: Cloud Run service.yaml + GKE kustomize tree

**Files:**
- Create: `infra/gcp/cloud-run/service.yaml`
- Create: `infra/gcp/gke/base/{deployment,service,configmap,kustomization}.yaml`
- Create: `infra/gcp/gke/overlays/prod/kustomization.yaml`

- [ ] **Step 1: `infra/gcp/cloud-run/service.yaml`**

```yaml
apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: deepcab-api
  annotations:
    run.googleapis.com/launch-stage: BETA
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/minScale: "0"
        autoscaling.knative.dev/maxScale: "10"
        run.googleapis.com/cpu-throttling: "false"
    spec:
      serviceAccountName: deepcab-runtime@__GCP_PROJECT__.iam.gserviceaccount.com
      containerConcurrency: 80
      timeoutSeconds: 60
      containers:
        - image: __IMAGE__
          ports:
            - name: http1
              containerPort: 8000
          resources:
            limits:
              cpu: "2"
              memory: 2Gi
          env:
            - name: APP_ENV
              value: prod
            - name: PORT
              value: "8000"
            - name: MLFLOW_TRACKING_URI
              value: __MLFLOW_URL__
            - name: MODEL_TARGET
              value: gcs
            - name: GCP_PROJECT
              value: __GCP_PROJECT__
            - name: MODEL_URI
              value: __MODEL_URI__
          startupProbe:
            httpGet:
              path: /healthz
              port: 8000
            failureThreshold: 30
            periodSeconds: 2
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8000
            periodSeconds: 10
  traffic:
    - percent: 100
      latestRevision: true
```

The placeholders `__IMAGE__`, `__GCP_PROJECT__`, `__MLFLOW_URL__`, `__MODEL_URI__` are `sed`-replaced at deploy time.

- [ ] **Step 2: `infra/gcp/gke/base/deployment.yaml`**

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: deepcab-api
spec:
  replicas: 2
  selector:
    matchLabels: { app: deepcab-api }
  template:
    metadata:
      labels: { app: deepcab-api }
    spec:
      serviceAccountName: deepcab-runtime
      containers:
        - name: api
          image: us-central1-docker.pkg.dev/PROJECT/deepcab/api:latest   # overlay overrides
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef: { name: deepcab-config }
          readinessProbe:
            httpGet: { path: /readyz, port: 8000 }
            periodSeconds: 5
          livenessProbe:
            httpGet: { path: /healthz, port: 8000 }
            periodSeconds: 10
          resources:
            requests: { cpu: "500m", memory: "512Mi" }
            limits:   { cpu: "2",    memory: "2Gi" }
```

- [ ] **Step 3: `infra/gcp/gke/base/service.yaml`**

```yaml
apiVersion: v1
kind: Service
metadata:
  name: deepcab-api
spec:
  type: ClusterIP
  selector: { app: deepcab-api }
  ports:
    - port: 80
      targetPort: 8000
```

- [ ] **Step 4: `infra/gcp/gke/base/configmap.yaml`**

```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: deepcab-config
data:
  APP_ENV: "prod"
  MLFLOW_TRACKING_URI: "http://mlflow.deepcab.svc.cluster.local:5000"
  MODEL_TARGET: "gcs"
```

- [ ] **Step 5: `infra/gcp/gke/base/kustomization.yaml`**

```yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
```

- [ ] **Step 6: `infra/gcp/gke/overlays/prod/kustomization.yaml`**

```yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: deepcab-prod
resources:
  - ../../base
images:
  - name: us-central1-docker.pkg.dev/PROJECT/deepcab/api
    newName: us-central1-docker.pkg.dev/PROJECT/deepcab/api
    newTag: latest   # overridden by CI via `kustomize edit set image ...`
```

- [ ] **Step 7: Verify with kustomize**

```bash
kustomize build infra/gcp/gke/overlays/prod | head -40
```

Expected: a rendered Deployment + Service + ConfigMap under namespace `deepcab-prod`. (Skip if kustomize not installed locally — CI will validate.)

---

## Task C2: Workload Identity Federation README + bootstrap.sh

**Files:**
- Create: `infra/gcp/workload-identity/README.md`
- Create: `infra/gcp/workload-identity/bootstrap.sh`

- [ ] **Step 1: Write `README.md`**

```markdown
# Workload Identity Federation bootstrap

One-time setup so GitHub Actions can deploy to GCP **without long-lived JSON keys**.

## Prerequisites

- `gcloud` CLI authenticated as a project owner.
- A GCP project with billing enabled.
- The deepCab repo on GitHub (note the org + repo name).

## Variables

```bash
export PROJECT=deepcab-prod-xxxx          # your GCP project id
export PROJECT_NUMBER=123456789012        # gcloud projects describe $PROJECT --format='value(projectNumber)'
export GH_OWNER=juan-garassino            # your github org/user
export GH_REPO=deepCab                    # your github repo (the parent — adjust if monorepo)
export REGION=us-central1
```

## Steps

```bash
# 1. Enable required APIs
gcloud services enable \
  iamcredentials.googleapis.com \
  artifactregistry.googleapis.com \
  run.googleapis.com \
  container.googleapis.com \
  iam.googleapis.com \
  --project=$PROJECT

# 2. Create Artifact Registry repo for the api image
gcloud artifacts repositories create deepcab \
  --repository-format=docker \
  --location=$REGION \
  --description="deepCab images" \
  --project=$PROJECT

# 3. Create the Workload Identity Pool + Provider for GitHub Actions
gcloud iam workload-identity-pools create github-pool \
  --project=$PROJECT --location=global \
  --display-name="GitHub Actions Pool"

gcloud iam workload-identity-pools providers create-oidc github-provider \
  --project=$PROJECT --location=global \
  --workload-identity-pool=github-pool \
  --display-name="GitHub Actions" \
  --attribute-mapping="google.subject=assertion.sub,attribute.repository=assertion.repository,attribute.repository_owner=assertion.repository_owner" \
  --attribute-condition="assertion.repository_owner == '$GH_OWNER'" \
  --issuer-uri="https://token.actions.githubusercontent.com"

# 4. Create the deployer service account
gcloud iam service-accounts create deepcab-deployer \
  --project=$PROJECT \
  --display-name="deepCab GH Actions deployer"

# 5. Grant deploy roles
for ROLE in roles/run.admin \
            roles/container.admin \
            roles/artifactregistry.writer \
            roles/iam.serviceAccountUser \
            roles/storage.objectAdmin ; do
  gcloud projects add-iam-policy-binding $PROJECT \
    --member="serviceAccount:deepcab-deployer@$PROJECT.iam.gserviceaccount.com" \
    --role="$ROLE"
done

# 6. Bind GitHub repo to the deployer SA via WIF
gcloud iam service-accounts add-iam-policy-binding \
  deepcab-deployer@$PROJECT.iam.gserviceaccount.com \
  --project=$PROJECT \
  --role=roles/iam.workloadIdentityUser \
  --member="principalSet://iam.googleapis.com/projects/$PROJECT_NUMBER/locations/global/workloadIdentityPools/github-pool/attribute.repository/$GH_OWNER/$GH_REPO"

# 7. Create the runtime service account used by Cloud Run / GKE
gcloud iam service-accounts create deepcab-runtime \
  --project=$PROJECT \
  --display-name="deepCab runtime SA"

for ROLE in roles/storage.objectViewer \
            roles/aiplatform.user \
            roles/logging.logWriter \
            roles/monitoring.metricWriter ; do
  gcloud projects add-iam-policy-binding $PROJECT \
    --member="serviceAccount:deepcab-runtime@$PROJECT.iam.gserviceaccount.com" \
    --role="$ROLE"
done

# 8. Allow the deployer SA to impersonate the runtime SA
gcloud iam service-accounts add-iam-policy-binding \
  deepcab-runtime@$PROJECT.iam.gserviceaccount.com \
  --project=$PROJECT \
  --role=roles/iam.serviceAccountUser \
  --member="serviceAccount:deepcab-deployer@$PROJECT.iam.gserviceaccount.com"

# 9. Print the values you need for GitHub variables
echo ""
echo "== Set these as repo variables (gh variable set) =="
echo "GCP_PROJECT=$PROJECT"
echo "GCP_PROJECT_NUMBER=$PROJECT_NUMBER"
echo "GCP_REGION=$REGION"
echo "GCP_WIF_PROVIDER=projects/$PROJECT_NUMBER/locations/global/workloadIdentityPools/github-pool/providers/github-provider"
echo "GCP_DEPLOYER_SA=deepcab-deployer@$PROJECT.iam.gserviceaccount.com"
echo ""
echo "== Set as repo SECRET =="
echo "SLACK_WEBHOOK_URL=<your incoming webhook URL>"
```

## GitHub variables + secrets to set

```bash
gh variable set GCP_PROJECT --body "$PROJECT"
gh variable set GCP_PROJECT_NUMBER --body "$PROJECT_NUMBER"
gh variable set GCP_REGION --body "$REGION"
gh variable set GCP_WIF_PROVIDER --body "projects/$PROJECT_NUMBER/locations/global/workloadIdentityPools/github-pool/providers/github-provider"
gh variable set GCP_DEPLOYER_SA --body "deepcab-deployer@$PROJECT.iam.gserviceaccount.com"

gh secret set SLACK_WEBHOOK_URL    # paste the URL when prompted
```

## Verification

After the first push of a `v*` tag, watch `.github/workflows/deploy-cloud-run.yml` succeed. If WIF is misconfigured the auth step fails with a clear message like "Permission 'iam.serviceAccounts.getAccessToken' denied".
```

- [ ] **Step 2: Write `bootstrap.sh`**

The same commands, wrapped as a script with `set -euo pipefail`. (Copy-paste the gcloud blocks. Add `[[ -z "${PROJECT:-}" ]] && { echo "set PROJECT first"; exit 1; }` at the top.)

- [ ] **Step 3: Add Makefile target**

```make
wif_bootstrap:
	@echo "Set PROJECT, PROJECT_NUMBER, GH_OWNER, GH_REPO, REGION env vars first."
	bash infra/gcp/workload-identity/bootstrap.sh
```

---

## Task C3: Reusable build-and-push workflow

**Files:**
- Create: `.github/workflows/build-and-push.yml`

- [ ] **Step 1: Write the reusable workflow**

```yaml
name: build-and-push

on:
  workflow_call:
    inputs:
      tag:
        description: "Image tag (e.g. v1.2.3 or sha-abc1234)"
        required: true
        type: string
    outputs:
      image:
        description: "Fully qualified image URI"
        value: ${{ jobs.build.outputs.image }}

permissions:
  id-token: write
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      image: ${{ steps.image.outputs.uri }}
    steps:
      - uses: actions/checkout@v4

      - id: gcp-auth
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ vars.GCP_WIF_PROVIDER }}
          service_account: ${{ vars.GCP_DEPLOYER_SA }}

      - uses: google-github-actions/setup-gcloud@v2

      - name: Configure docker for GAR
        run: gcloud auth configure-docker ${{ vars.GCP_REGION }}-docker.pkg.dev --quiet

      - id: image
        run: |
          IMG=${{ vars.GCP_REGION }}-docker.pkg.dev/${{ vars.GCP_PROJECT }}/deepcab/api:${{ inputs.tag }}
          echo "uri=$IMG" >> "$GITHUB_OUTPUT"

      - name: Build, tag, and push image to Amazon ECR
        # NOTE: step name kept similar to the AWS reference for muscle memory.
        run: |
          docker buildx build \
            --file infra/docker/Dockerfile \
            --tag "${{ steps.image.outputs.uri }}" \
            --push \
            --build-arg GPU=0 \
            ./001-deepCab-api
```

Path note: the build context is the api package directory because that's where `pyproject.toml` + `deepCab/` live. If this workflow lives in a single-package repo (the api package itself is the repo root), drop `./001-deepCab-api` → `.`.

---

## Task C4: deploy-cloud-run.yml (mirror the screenshot)

**Files:**
- Create: `.github/workflows/deploy-cloud-run.yml`

- [ ] **Step 1: Write the workflow**

```yaml
name: deploy

# Mirrors the user's reference screenshot:
# Pull slack-notify → Checkout → Slack start → Configure GCP creds (OIDC) →
# Login to GAR → Build, tag, push → Render Cloud Run spec →
# Deploy to Cloud Run → Slack success/failure.

on:
  push:
    tags: ['v*']
  workflow_dispatch:
    inputs:
      tag:
        description: "Image tag to deploy"
        required: true
        default: "latest"
      model_uri:
        description: "Optional GCS URI of the model artifact"
        required: false
        default: ""

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Slack — deploy starting
        uses: rtcamp/action-slack-notify@v2.3.3
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK_URL }}
          SLACK_TITLE: "[ci] deploy → Cloud Run starting"
          SLACK_MESSAGE: "ref=${{ github.ref_name }} sha=${{ github.sha }}"
          SLACK_COLOR: "#cccccc"
          MSG_MINIMAL: actions url

      - name: Configure AWS credentials (OIDC)
        # name kept similar to the AWS reference; under the hood it's GCP.
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ vars.GCP_WIF_PROVIDER }}
          service_account: ${{ vars.GCP_DEPLOYER_SA }}

      - uses: google-github-actions/setup-gcloud@v2

      - name: Login to Amazon ECR
        # AWS-style step name for parity; actually GAR.
        run: gcloud auth configure-docker ${{ vars.GCP_REGION }}-docker.pkg.dev --quiet

      - name: Build, tag, and push image to Amazon ECR
        id: build
        run: |
          TAG="${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref_name }}"
          IMG="${{ vars.GCP_REGION }}-docker.pkg.dev/${{ vars.GCP_PROJECT }}/deepcab/api:${TAG}"
          docker buildx build \
            --file infra/docker/Dockerfile \
            --tag "$IMG" \
            --push \
            --build-arg GPU=0 \
            .
          echo "image=$IMG" >> "$GITHUB_OUTPUT"

      - name: Render ECS task definition (inject new image)
        # AWS-style step name for parity; actually Cloud Run KRM.
        run: |
          sed -e "s|__IMAGE__|${{ steps.build.outputs.image }}|g" \
              -e "s|__GCP_PROJECT__|${{ vars.GCP_PROJECT }}|g" \
              -e "s|__MLFLOW_URL__|${{ vars.MLFLOW_URL }}|g" \
              -e "s|__MODEL_URI__|${{ inputs.model_uri }}|g" \
              infra/gcp/cloud-run/service.yaml > service.rendered.yaml
          cat service.rendered.yaml

      - name: Deploy ECS task definition to service
        # AWS-style step name; actually `gcloud run services replace`.
        run: |
          gcloud run services replace service.rendered.yaml \
            --region=${{ vars.GCP_REGION }} \
            --platform=managed \
            --project=${{ vars.GCP_PROJECT }}

      - name: Slack — deploy success
        if: success()
        uses: rtcamp/action-slack-notify@v2.3.3
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK_URL }}
          SLACK_COLOR: good
          SLACK_TITLE: "[ci] deploy → Cloud Run ✓"
          SLACK_MESSAGE: "${{ steps.build.outputs.image }}"

      - name: Slack — deploy failure
        if: failure()
        uses: rtcamp/action-slack-notify@v2.3.3
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK_URL }}
          SLACK_COLOR: danger
          SLACK_TITLE: "[ci] deploy → Cloud Run ✗"
          SLACK_MESSAGE: "see ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
```

Step names use AWS-style wording where they line up with the screenshot ("Login to Amazon ECR" etc.) — comments explain it's GCP underneath. If you'd rather not LARP the AWS naming, rename the steps to "Login to Artifact Registry", "Render Cloud Run service spec", "Deploy to Cloud Run" — the screenshot parity is a nice-to-have, not a requirement.

---

## Task C5: deploy-gke.yml (dispatch-only)

**Files:**
- Create: `.github/workflows/deploy-gke.yml`

- [ ] **Step 1: Write the workflow**

```yaml
name: deploy-gke

# Dispatch-only by default to avoid standing GKE cluster cost.
# Flip the trigger to `push: tags: ['v*']` once you're paying for a cluster.

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Image tag to deploy"
        required: true
        default: "latest"
      cluster:
        description: "GKE cluster name"
        required: true
      cluster_region:
        description: "GKE cluster region"
        required: true
        default: "us-central1"

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Slack — deploy starting
        uses: rtcamp/action-slack-notify@v2.3.3
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK_URL }}
          SLACK_TITLE: "[ci] deploy → GKE starting"
          SLACK_MESSAGE: "cluster=${{ inputs.cluster }} tag=${{ inputs.tag }}"
          SLACK_COLOR: "#cccccc"

      - uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ vars.GCP_WIF_PROVIDER }}
          service_account: ${{ vars.GCP_DEPLOYER_SA }}

      - uses: google-github-actions/setup-gcloud@v2
        with:
          install_components: 'gke-gcloud-auth-plugin'

      - name: Configure docker for GAR
        run: gcloud auth configure-docker ${{ vars.GCP_REGION }}-docker.pkg.dev --quiet

      - id: build
        name: Build + push image
        run: |
          IMG="${{ vars.GCP_REGION }}-docker.pkg.dev/${{ vars.GCP_PROJECT }}/deepcab/api:${{ inputs.tag }}"
          docker buildx build \
            --file infra/docker/Dockerfile \
            --tag "$IMG" \
            --push \
            --build-arg GPU=0 \
            .
          echo "image=$IMG" >> "$GITHUB_OUTPUT"

      - name: Get GKE credentials
        run: |
          gcloud container clusters get-credentials ${{ inputs.cluster }} \
            --region=${{ inputs.cluster_region }} \
            --project=${{ vars.GCP_PROJECT }}

      - name: Set image in overlay
        run: |
          cd infra/gcp/gke/overlays/prod
          kustomize edit set image \
            us-central1-docker.pkg.dev/PROJECT/deepcab/api=${{ steps.build.outputs.image }}

      - name: kustomize build → kubectl apply
        run: |
          kustomize build infra/gcp/gke/overlays/prod | kubectl apply -f -

      - name: Wait for rollout
        run: |
          kubectl -n deepcab-prod rollout status deployment/deepcab-api --timeout=5m

      - name: Slack — success
        if: success()
        uses: rtcamp/action-slack-notify@v2.3.3
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK_URL }}
          SLACK_COLOR: good
          SLACK_TITLE: "[ci] deploy → GKE ✓"
          SLACK_MESSAGE: "${{ steps.build.outputs.image }}"

      - name: Slack — failure
        if: failure()
        uses: rtcamp/action-slack-notify@v2.3.3
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK_URL }}
          SLACK_COLOR: danger
          SLACK_TITLE: "[ci] deploy → GKE ✗"
          SLACK_MESSAGE: "see ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
```

---

## Task C6: Update ci.yml paths

**Files:**
- Modify: `.github/workflows/ci.yml`

- [ ] **Step 1: Update compose-integration step**

Find the `compose-integration` job in `ci.yml`. Replace:

```yaml
- name: Bring up core stack
  run: docker compose up -d --build api mlflow postgres minio redis
```

with:

```yaml
- name: Bring up core stack
  run: docker compose -f infra/compose/docker-compose.yml up -d --build api mlflow postgres minio redis
```

And:

```yaml
- name: Tear down
  if: always()
  run: docker compose down -v
```

with:

```yaml
- name: Tear down
  if: always()
  run: docker compose -f infra/compose/docker-compose.yml down -v
```

- [ ] **Step 2: Verify**

```bash
grep -n "docker compose" .github/workflows/ci.yml
```

Expected: every line includes `-f infra/compose/...`.

---

## Task C7: Update CLAUDE.md, README.md

**Files:**
- Modify: `CLAUDE.md`
- Modify: `README.md`

- [ ] **Step 1: CLAUDE.md — add CI/CD subsection**

After the "Common commands" section, add:

```markdown
## CI/CD

GitHub Actions workflows under `.github/workflows/`:

| Workflow | Trigger | Purpose |
|---|---|---|
| `ci.yml` | PR, push to main | lint + tests + path-gated compose smoke |
| `train-smoke.yml` | PR (paths under `deepCab/{models,features,training,data,schemas}`) | 6-backend × 1k synth training smoke |
| `nightly-bench.yml` | cron 06:00 UTC | Optuna 10k bench |
| `build-and-push.yml` | reusable | OIDC → GAR build + push |
| `deploy-cloud-run.yml` | `v*` tag; `workflow_dispatch` | **default deploy**; build/push + render+replace Cloud Run service.yaml + Slack |
| `deploy-gke.yml` | `workflow_dispatch` only | build/push + kustomize apply + Slack |

Workflow Identity Federation bootstrap is documented in `infra/gcp/workload-identity/README.md`. GitHub variables: `GCP_PROJECT`, `GCP_PROJECT_NUMBER`, `GCP_REGION`, `GCP_WIF_PROVIDER`, `GCP_DEPLOYER_SA`, `MLFLOW_URL`. Secret: `SLACK_WEBHOOK_URL`. No long-lived GCP keys.
```

- [ ] **Step 2: README.md — Deploy section**

Add a "Deploy" subsection after "Run":

```markdown
## Deploy to GCP Cloud Run

1. One-time WIF bootstrap: `make wif_bootstrap` (after setting `PROJECT`, `PROJECT_NUMBER`, `GH_OWNER`, `GH_REPO`, `REGION` env vars).
2. Push a tag: `git tag v0.1.0 && git push --tags`.
3. Watch the Slack channel `#deepcab-ops` for `[ci] deploy → Cloud Run ✓`.

GKE deploys are dispatch-only:
```bash
gh workflow run deploy-gke.yml -f tag=v0.1.0 -f cluster=deepcab-prod -f cluster_region=us-central1
```
```

---

## Task C8: Commit Sub-project C

- [ ] **Step 1: Stage + commit**

```bash
git add -A
git status --short
git commit -m "$(cat <<'EOF'
feat(ci): GCP CI/CD with Workload Identity Federation + Slack notifications

- .github/workflows/build-and-push.yml — reusable OIDC → GAR build/push
- .github/workflows/deploy-cloud-run.yml — default deploy on v* tag; mirrors the user's reference screenshot (build → push → render → deploy → Slack ✓/✗)
- .github/workflows/deploy-gke.yml — workflow_dispatch only (avoids standing GKE cost)
- infra/gcp/cloud-run/service.yaml — Knative service KRM template (__IMAGE__ + __MODEL_URI__ placeholders)
- infra/gcp/gke/{base,overlays/prod} — kustomize tree
- infra/gcp/workload-identity/README.md + bootstrap.sh — one-time gcloud commands
- Makefile: make wif_bootstrap
- ci.yml: compose paths now point at infra/compose/

GitHub variables: GCP_PROJECT, GCP_PROJECT_NUMBER, GCP_REGION,
  GCP_WIF_PROVIDER, GCP_DEPLOYER_SA, MLFLOW_URL.
GitHub secret: SLACK_WEBHOOK_URL only — no long-lived keys.

Sub-project C of the GCP infra design.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Done criteria

- [x] `build-and-push.yml` is a callable reusable workflow
- [x] `deploy-cloud-run.yml` fires on `v*` tag and `workflow_dispatch`
- [x] `deploy-gke.yml` is `workflow_dispatch`-only
- [x] All three deploy steps fire Slack notifications via `rtcamp/action-slack-notify@v2.3.3`
- [x] `infra/gcp/cloud-run/service.yaml` exists with `__IMAGE__` + `__GCP_PROJECT__` + `__MLFLOW_URL__` + `__MODEL_URI__` placeholders
- [x] `infra/gcp/gke/{base,overlays/prod}` is a valid kustomize tree
- [x] `infra/gcp/workload-identity/README.md` + `bootstrap.sh` document one-time setup
- [x] `ci.yml` uses `infra/compose/` paths
- [x] CLAUDE.md + README.md updated
- [x] One commit

**Manual verification** (user, post-merge): Set GitHub variables/secret, push `v0.1.0` tag, confirm Slack `[ci] deploy → Cloud Run ✓` arrives.
