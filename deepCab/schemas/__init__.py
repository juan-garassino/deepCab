"""Canonical Pydantic schemas — single source of truth.

API bodies (api.py), agent tool params (agent.py + config.py), Hydra structured
configs (config.py), pydantic-settings (settings.py), and MLflow param logs all
derive from this package."""
