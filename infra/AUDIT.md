# AUDIT — 2026-06-04

Sub-project E of the 2026-06-03 GCP infra design. Full code audit + targeted
simplifications. Phase A captures automated baselines (radon CC/MI/raw, vulture
dead-code, pylint duplicate-code, pydeps cycles). Phase B applies six manual
simplifications and re-measures.

## Phase A — automated baseline

| Tool | Output | Findings (raw) |
|---|---|---|
| radon (CC + MI + raw) | `infra/audit/AUDIT.before.txt` | 3 functions at C/D complexity (out of 375 blocks); avg CC = A (2.088) |
| vulture (dead code) | `infra/audit/dead.txt` | 10 candidates, 2 after filtering pytest-fixture false positives |
| pylint (duplicate-code) | `infra/audit/dup.txt` | 0 duplicate blocks at `--min-similarity-lines=8` |
| pydeps (cycles) | `infra/audit/cycles.skip.txt` | skipped (graphviz unavailable); pylint `cyclic-import` reports 0 cycles |

### radon hotspots (cyclomatic complexity >= C)

| Function | File | CC |
|---|---|---|
| `Batcher._run` | `deepCab/serving/batcher.py:66` | C (13) |
| `run_improve` | `deepCab/agent/improve.py:36` | D (21) |
| `run_one_turn` | `deepCab/agent/executor.py:51` | D (27) |

These three are inherently stateful: the batcher is a coroutine that coalesces
async submits; the agent functions implement multi-step OpenAI tool-call loops
with budget + idempotency + circuit-breaker concerns. Splitting them further
would push the loop-control state into helpers without simplifying the call
graph — **deferred** (see below).

### vulture findings (10 raw, 2 after triage)

| Finding | Disposition | Reason |
|---|---|---|
| `deepCab/grpc/deepcab_pb2_grpc.py:4 unused import 'warnings'` | deferred | Auto-generated by `protoc`; do not hand-edit. Regenerate via `make grpc_gen` to clear. |
| `deepCab/models/base.py:39 unused variable 'deep'` | rejected | Required by sklearn `BaseEstimator.get_params(deep=True)` API contract — sklearn introspects the signature. |
| 8x `tests/security/.../fresh_settings`  | rejected | `pytest` fixtures consumed by autouse side-effect (clear settings cache); vulture cannot see fixture-injection semantics. |

### pylint duplicate-code

No duplicate blocks at `--min-similarity-lines=8`. Code rated 10.00/10. Spec
limit was met without simplification.

### pydeps cycles

`pydeps` requires `graphviz` to render PNG, which is unavailable on this host
(see `infra/audit/cycles.skip.txt`). As a fallback, `pylint --enable=cyclic-import`
was run over `deepCab/` and reported **0 cycles**. The import graph stays acyclic.

## Phase B — manual triage + actions

### B.1 — Registry dispatcher legacy collapse

| Finding | Action | Result |
|---|---|---|
| `save_artifact` (tempdir-based) + `load_artifact` (factory-via-build_estimator) coexisted with the persistent `save_full_state` / `load_state_from_disk`; only caller was `training/train.py` which wrote weights twice (once to a tempdir for MLflow, once to the persistent runs/<id>/model dir). | Reordered the `_run_training` block so `save_full_state` runs first, then point MLflow's `log_artifacts` and the ONNX export at `runs/<id>/model/`. Deleted both legacy functions, dropped `tempfile`/`build_estimator` imports from dispatcher.py, removed re-exports from `registry/__init__.py`. | -18 LOC in `deepCab/registry/dispatcher.py`; deleted ~4 LOC of dead callers in `training/train.py` + outdated comment in `api/routers/train.py`. No test regression (32 tests in registry+training+api green). |

### B.2 — `schemas/settings._maybe_read_file`

| Finding | Action | Result |
|---|---|---|
| The `file:` docker-secrets URI resolver was a top-level helper called from three identical `@field_validator(..., mode='before')` blocks (`ObsSettings.slack_webhook_url`, `OpenAISettings.api_key`, `DeepCabSettings.api_key`). Per-field validators meant every new secret field needed a new validator. | Replaced the helper + three validators with a single `FileUriEnvSettingsSource(EnvSettingsSource)` that intercepts `get_field_value` and rewrites `file:` URIs at the source layer. Wired into each affected sub-settings via a shared `_customise_sources(...)` helper called from each `settings_customise_sources` classmethod. | New surface (+30 LOC) is more idiomatic for pydantic-settings ≥ 2.4 (we have 2.14); old surface (−28 LOC) is gone. Net ~+2 LOC, but **every future secret field auto-inherits** the `file:` resolution. +2 regression tests in `tests/schemas/test_file_uri.py`. One pre-existing test updated: pydantic-settings wraps the `FileNotFoundError` in `SettingsError`, so `test_file_prefix_raises_when_missing` now asserts on the exception chain via `__cause__`. |

### B.3 — Agent trace writers

| Finding | Action |
|---|---|
| Reviewed every `trace.append(...)` site in `agent/executor.py`, `agent/improve.py`, `agent/trace.py`. Mapped event kinds: `budget`, `tool_call`, `tool_result`, `plan`, `note`, `error`, `done`, `llm`. The `budget` exhausted event appears in both `executor.run_one_turn` and `improve.run_improve` — but these are **scoped** (one for the inner tool loop, one for the outer iteration loop) and either may trip first depending on which counter hits its cap, so both are required. No duplicate writers for any other event class. | **Rejected** — no duplicate writers; the two `budget` writes serve distinct scopes. Existing test `tests/agent/test_budget.py::test_restore_rebuilds_from_trace` already covers `Budget.restore` from a JSONL trace, so no new test was added. |

### B.4 — Preprocess vs features/pipeline boundary

| Finding | Action |
|---|---|
| `training/preprocess.py` is 62 LOC: `clean()` (NYC-bound polars filter), `load()` (scan + clean), `featurize()` (calls `features.pipeline.preprocess_features`), `preprocess()` (load + featurize entrypoint). `features/pipeline.py::preprocess_features` is a pure `pd.DataFrame -> (N, 65) np.ndarray` ColumnTransformer. The boundary is already correct: preprocess orchestrates I/O + cleaning then delegates feature engineering to `features.pipeline`. Module-level docstrings already document this contract. | **Accepted (no code change)** — boundary is correctly drawn and documented. The Plan F additions (`_run_training`, `_push_to_gcs`) live in `training/train.py` and don't touch this boundary. No-op. |

### B.5 — Router thinness

| Router | LOC before | LOC after | Extracted service |
|---|---|---|---|
| `predict.py`  | 107 | 107 | **rejected** — two private helpers (`_point_predict`, `_predict_with_interval`) at ~30 LOC together drive all three endpoints; extracting would only move them to a service module that has exactly one consumer (this router). |
| `explain.py`  | 48  | 48  | n/a (under threshold) |
| `train.py`    | 79  | 78  | n/a (under threshold) — but B.1 cleaned an outdated `save_artifact` comment |
| `agent.py`    | 100 | 100 | n/a (at threshold; thin) |
| `meta.py`     | 21  | 21  | n/a |
| `monitor.py`  | 25  | 25  | n/a |
| `graphql.py`  | 142 | 142 | **rejected** — file is dominated by strawberry schema declarations (`@strawberry.type`, `@strawberry.field`) which cannot be meaningfully relocated; the only `Query.predict` and `Mutation.train` bodies are short and call into existing services (`predict_one`, `run_train`). Extracting would add an indirection without simplifying. |

### B.6 — Fixture consolidation

| Fixture | Was in | Now in |
|---|---|---|
| `_clean_state` (autouse) | `tests/api/test_routers.py`, `tests/api/test_graphql.py` (two divergent copies — routers reset model+tasks, graphql reset model only) | `tests/api/conftest.py` (single copy, strictest semantics — resets `STATE.model` + `STATE.tasks`) |

Other fixtures inspected (`_reset_settings_cache` in root `tests/conftest.py`, `fresh_settings` in `tests/security/conftest.py` style, `client()` in `tests/api/test_routers.py`) are each used by a single suite, so per the plan's "skip single-use fixtures" rule, none were hoisted.

No `BACKENDS` constant was duplicated in tests — backend parametrization in this repo is per-test where the list of supported backends is small and locally documented.

## Rejected findings (with reasons)

- **vulture: `deepCab/models/base.py:39 deep`** — kept because sklearn's `BaseEstimator.get_params` API contract requires the `deep` keyword by signature inspection.
- **vulture: `tests/security/.../fresh_settings`** (8 occurrences) — kept because they are pytest fixture parameters with autouse side-effects; vulture cannot model fixture injection.
- **B.3 duplicate `budget` trace writers** — kept because the inner (tool-loop) and outer (improve-loop) `BudgetExhausted` paths fire in different scopes and both must record the exhaustion event.
- **B.5 router extraction for `predict.py` / `graphql.py`** — kept because extracting helpers that have exactly one consumer adds indirection without simplifying.
- **radon CC hotspots** (`run_one_turn`, `run_improve`, `Batcher._run`) — kept because each is an inherently stateful loop (OpenAI tool-call orchestration, async batch coalescing) where splitting would push loop state into helpers without reducing call-graph complexity.

## Deferred findings

- **vulture: `deepCab/grpc/deepcab_pb2_grpc.py:4 unused 'warnings'`** — auto-generated by `protoc`; not hand-edited. Will clear after the next `make grpc_gen`.

## Delta

| Metric | Before | After | Δ |
|---|---|---|---|
| LOC (`deepCab/`) | 6893 | 6891 | -2 |
| SLOC (`deepCab/`) | 4558 | 4556 | -2 |
| Blocks analyzed | 375 | 375 | 0 |
| Avg cyclomatic complexity | A (2.088) | A (2.109) | +0.021 (still A) |
| C/D complexity hotspots | 3 | 3 | 0 (same three — all deferred) |
| pylint duplicate-code blocks | 0 | 0 | 0 |
| Cyclic imports | 0 | 0 | 0 |
| Tests passing | 114 | 116 | +2 (new `test_file_uri.py`) |
| Tests skipped | 13 | 13 | 0 |
| New runtime dependencies | — | 0 | 0 |
| New dev dependencies | — | 4 | +4 (radon, vulture, pylint, pydeps) |

The headline LOC delta is small because B.2 swapped one abstraction for another
(field-validator boilerplate → settings-source) at comparable line count. The
real win is the abstraction shift: every future secret field auto-inherits
`file:` URI resolution without new code. B.1 removes a genuine duplication
(persistent run-dir was being shadowed by a throwaway tempdir).

## Acceptance check

- [x] All tests pass (count = 116; was 114 before this sub-project)
- [x] No new runtime dependencies
- [x] Cyclomatic complexity did not regress on average (still A grade)
- [x] `infra/AUDIT.md` lists every finding with an action
- [x] Phase A artifacts committed under `infra/audit/`
- [x] B.1 legacy registry path collapsed
- [x] B.2 `_maybe_read_file` replaced with `FileUriEnvSettingsSource`
- [x] B.3 agent trace writers confirmed single-source per event class
- [x] B.4 preprocess ↔ features/pipeline boundary documented
- [x] B.5 every router < 100 LOC OR documented why extraction was rejected
- [x] B.6 duplicate `_clean_state` fixture hoisted to `tests/api/conftest.py`

---

# v1 Polish Addendum — 2026-06-04

Three parallel lanes ran on top of `d7d575b`. Spec: `docs/superpowers/specs/2026-06-04-deepcab-v1-polish.md`.

## Lane A — Schemas (Enums + Pydantic v2) — commit `3224b1d`

- New `deepCab/schemas/enums.py` with 14 `str + Enum` classes
- 11 inline `Literal[...]` sites migrated across `schemas/{agent,api,settings,config}.py`
- 6 per-backend `kind: Literal["..."]` discriminators preserved (structural; not values)
- Pydantic v2 sweep: 0 `class Config:` blocks found (already on `ConfigDict`); 0 `.dict()`/`.parse_obj()`/`.json()` calls in schemas; 2 bare `Field(...)` → `Annotated[T, Field(...)]`
- 40 new tests (`tests/schemas/test_enums.py`)

## Lane B — Services + Providers — commit `6feeae5`

Router LOC trimmed (business logic moved to services):

| Router | Before | After | Δ |
|---|---|---|---|
| `predict.py` | 107 | 48 | -59 |
| `explain.py` | 49 | 37 | -12 |
| `train.py` | 80 | 54 | -26 |
| `agent.py` | 101 | 55 | -46 |
| `graphql.py` | 142 | 142 | 0 (Strawberry resolvers; deferred — would touch 100+ LOC of GraphQL-specific glue) |
| `meta.py` / `monitor.py` | 21 / 25 | 21 / 25 | 0 (already thin) |

- 4 services created (`PredictionService`, `ExplanationService`, `TrainingService`, `AgentService`)
- 3 provider Protocols + 6 concrete impls (`SlackProvider`, `ModelHandleProvider`, `TraceProvider`)
- `api/deps.py` refactored as a factory tray
- 24 new tests (12 service unit + 12 provider strategy)

## Lane C — CLI / Commands — commit `20bfcf3`

- New `deepCab/cli/{__init__,train,predict,migrate,agent,serve,status}.py` + `deepCab/__main__.py`
- `pyproject.toml` adds `typer>=0.12.0` + `[project.scripts] deepcab = "deepCab.cli:app"`
- Makefile: 6 new `cli_*` shortcut targets
- 16 new tests (`tests/cli/`)
- Legacy entrypoints (`python -m deepCab.training.train`, `python -m deepCab.agent.cli`, `python -m deepCab.data.migrate`) preserved

## Combined polish delta

| Metric | Pre-polish (`d7d575b`) | Post-polish (`20bfcf3`) | Δ |
|---|---|---|---|
| Tests passing | 116 | 178 | +62 |
| Tests added | — | 80 | +80 (18 new tests reclassified as skipped due to import-env gates) |
| New runtime deps | — | `typer>=0.12.0` | +1 |
| Routers total LOC | 525 | 382 | -143 |
| Services + providers LOC | 0 | ~530 | +530 (was inline; now organized) |

Net: cleaner separation of concerns, friendlier CLI, exhaustive enums replacing magic strings. No HTTP contracts or env var names changed.

---

# Simplification Audit Pass — 2026-06-04 (post-polish)

After the v1 polish pass added ~+930 LOC, this audit ran 5 parallel `code-simplifier` subagents over disjoint subtrees of the `deepCab/` source tree. Goal: delete code that doesn't earn its place. Result: **-156 LOC** across 5 commits. 178 tests still passing (no regressions; 2 pre-existing `prefect`-import failures are env-only).

## Per-lane summary

| Lane | Owned subtrees | Commit | LOC Δ | Highlights |
|---|---|---|---|---|
| S1 | `schemas/` + `obs/` | `82ed677` | -15 | Drop `_positive_fare` (duplicate of `Field(gt=0)`); drop dead `obs/__init__.py` re-exports; drop `obs.otel.get_tracer()` wrapper |
| S2 | `data + features + registry + serving + flow_v2 + explain` | `7010a3d` | -49 | Drop unused `io.read()` + `io.duckdb_query()` (kills duckdb import); drop `__init__.py` F401 re-exports across 5 packages; drop `flow_v2.deploy_one_off()` (speculative, no callers); drop dead `DEEP_MLP_KINDS` constant |
| S3 | `models + training` | `388c420` | -20 | Drop stale `_ = Any` / `_ = BaseModel; _ = os` silencer pairs; collapse two-arm if into `if kind in (...)` in `onnx_export`; inline single-use `est_cls` in factory; `torch_mlp._eval` now delegates to `_predict`; drop duplicate `mlflow` import in `train.py` |
| S4 | `api + grpc + __main__` | `9e57542` | -46 | Drop `PredictionService.slack` field (never invoked); drop `get_slack_provider()` factory; drop `ModelDep` / `ApiKeyDep` Annotated aliases (zero callers); drop `TaskRecord.result` (write-only); drop `CollectorRegistry` re-export |
| S5 | `agent + cli` | `4e323d1` | -26 | Drop `tracer` param + dead branch from `agent.tools.dispatch()` + `agent.executor._call_tool_with_timeout` (only caller always passed `None`); drop `_started = time.time()` instrument-but-never-emit pair; drop unused `IterRecord` dataclass; drop orphaned `AgentTrace.completed_request_ids()` |

## What we didn't delete (and why)

Each lane's report has a "rejected" list. Highlights:

- **All 14 enums** (`schemas/enums.py`) — kept per spec §3 even single-consumer ones.
- **Hand-rolled OpenAI tool-call loop** (`agent/executor.py`) — CLAUDE.md mandate: no LangChain.
- **6 backend classes + `BACKENDS` plain-dict registry** — explicit learning surface (017 pattern).
- **Provider Protocols** (`SlackProvider`, `ModelHandleProvider`, `TraceProvider`) — kept though some impls now have no production callers (see Follow-ups below).
- **Pydantic boundary schemas** at `schemas/{api,config,data,registry,settings,agent}.py` — contracts.
- **`obs/jsonl.py`** vendored 017 tracer — preserved as a learning artifact.

## Race-condition note

S2 and S3 ran in parallel on the same git index. S2's first commit (`e93cd90`, now orphaned) accidentally absorbed S3's staged files. S2 detected this, ran `git reset HEAD~1`, restored the working tree, and recommitted as `7010a3d`. S3's changes were then committed by the integrator (`388c420`) using path-scoped `git commit -- deepCab/models/ deepCab/training/` to avoid swallowing S4's then-staged work.

**Lesson**: when dispatching parallel subagents that share a git index, each should snapshot the index (via `git stash --keep-index`) before staging or use scoped `git add <paths>` exclusively. Future audits should either serialize the commits or use git worktrees per lane.

## Follow-ups identified (NOT actioned)

These are real simplifications the audit found but the spec preserved:

1. **`SlackProvider` Protocol + 2 impls** (`api/providers.py`) — has zero production consumers after S4 removed `PredictionService.slack`. The actual Slack notifier is `obs/slack.py`. The Protocol could be deleted in a future tightening pass.
2. **`ModelHandleProvider.StateModelHandleProvider`** — only used by the Protocol's own tests. Same situation.
3. **`graphql.py` resolvers** (142 LOC of Strawberry glue) — could route the `predict` resolver through `PredictionService` for full DRY. Plan E and S4 both deferred.
4. **`cli/migrate.py`** — manipulates `sys.argv` to call `data/migrate.py:main`. Could be cleaner if `data/migrate.py` exposed a callable taking args directly. Cross-lane refactor; not done here.
5. **`Batcher._run`** cyclomatic complexity (Radon C) — pre-existing deferral.

## Cumulative LOC tally

| Pass | Date | LOC Δ |
|---|---|---|
| Plan E audit | 2026-06-04 (yesterday) | -2 |
| v1 polish (Lanes A/B/C) | 2026-06-04 | +930 (intentional growth) |
| Simplification audit (Lanes S1–S5) | 2026-06-04 (today) | **-156** |
| Net since v1 baseline | | +772 |

Most of the post-polish growth is the new CLI surface (Lane C) + service classes (Lane B). Today's audit recovered 17% of that. Without major scope changes (collapsing the CLI to a single file, removing GraphQL, etc.) further LOC reductions would compromise clarity or violate the preserve list.
