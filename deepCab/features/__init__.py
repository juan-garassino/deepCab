"""Stateless feature transforms (Polars-backed) + pipeline assembly.

Submodules are imported directly (e.g.
`from deepCab.features.pipeline import preprocess_features`);
no symbols are re-exported here."""
